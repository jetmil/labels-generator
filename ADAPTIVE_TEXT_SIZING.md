# Адаптивная подгонка текста и система предупреждений

**Дата:** 2025-12-04
**Версия:** 2.0

## Что добавлено

### 1. Адаптивный размер шрифта

Система автоматически уменьшает размер шрифта в зависимости от длины текста, чтобы всё помещалось на этикетке/инструкции.

#### Этикетки (labels):

**Название свечи:**
- До 15 символов: `15pt` (стандарт)
- 16-30 символов: `11pt` (long-title)
- 31+ символов: `9pt` (very-long-title)

**Описание:**
- Всегда обрезается на 5 строках с многоточием
- Размер: `7.5pt`

#### Инструкции (instruction cards):

**Заголовок:**
- До 20 символов: `18pt` (стандарт)
- 21-30 символов: `14pt` (long-title)
- 31+ символов: `11pt` (very-long-title)

**Описание:**
- До 100 символов: `9pt` (text-short/medium)
- 100-200 символов: `9pt` (text-medium)
- 200-300 символов: `8pt` (text-long)
- 300-400 символов: `7pt` (text-very-long)
- 400+ символов: `6.5pt` (text-overflow) ⚠️

**Практика:**
- До 150 символов: `9pt`
- 150-250 символов: `9pt`
- 250-350 символов: `8pt`
- 350-450 символов: `7pt`
- 450+ символов: `6.5pt` ⚠️

**Заговор:**
- До 100 символов: `8.5pt`
- 100-200 символов: `8.5pt`
- 200-280 символов: `7.5pt`
- 280-350 символов: `6.5pt`
- 350+ символов: `6pt` ⚠️

### 2. Система предупреждений

При генерации этикеток система проверяет переполнение текста и создаёт **первую страницу с предупреждениями**.

#### Критерии предупреждений:

**⚠ Название слишком длинное:**
- Более 30 символов
- Рекомендация: до 30 символов

**⚠ Описание слишком длинное:**
- Более 250 символов
- Рекомендация: до 250 символов
- На этикетке отображается только часть (5 строк)

**⚠ Практика слишком длинная:**
- Более 400 символов
- Рекомендация: до 400 символов
- Может не поместиться на инструкции

**⚠ Заговор слишком длинный:**
- Более 300 символов
- Рекомендация: до 300 символов
- Будет уменьшен до минимального размера

**⚠ Критическое переполнение инструкции:**
- Общий объём (описание + практика + заговор) > 800 символов
- **Высокий риск**: текст может вылезти за границы карточки

### 3. Страница предупреждений

Если хотя бы одна свеча имеет проблемы, генерируется дополнительная **первая страница** с предупреждениями.

**Формат:**
```
┌─────────────────────────────────────────────┐
│  ⚠ Предупреждения о переполнении текста     │
│                                             │
│  Найдено проблем в X свечах из Y            │
├─────────────────────────────────────────────┤
│  СВЕЧА ОЧИЩЕНИЯ ПРОСТРАНСТВА                │
│  ⚠ Название слишком длинное (35 символов)   │
│  ⚠ Практика слишком длинная (450 символов)  │
├─────────────────────────────────────────────┤
│  СВЕЧА ЗАЩИТЫ ОТ НЕГАТИВА                   │
│  ⚠ Общий объём критический (850 символов)   │
└─────────────────────────────────────────────┘
```

## Как использовать

### 1. Генерация этикеток

```
1. Выберите свечи в каталоге
2. Нажмите "Печать этикеток (N)"
3. Нажмите "Открыть для печати" или "Скачать HTML"
```

Если есть предупреждения:
- **Первая страница**: список всех проблем
- **Следующие страницы**: этикетки (9 шт/страница)
- **Последние страницы**: инструкции (4 шт/страница)

### 2. Рекомендации по тексту

**Для идеального результата:**

✅ **Название:** 10-25 символов
- Пример: "СВЕЧА ЛЮБВИ" (12 символов)

✅ **Описание:** 100-200 символов
- ~2-3 предложения
- Короткая суть

✅ **Практика:** 150-350 символов
- ~3-5 предложений
- Конкретные инструкции

✅ **Заговор:** 80-250 символов
- ~2-4 строки
- Ритмичный текст

### 3. Что делать с предупреждениями

**⚠ Если видите предупреждение:**

1. **Откройте свечу в редакторе**
2. **Сократите текст** до рекомендуемого объёма:
   - Уберите повторы
   - Разбейте на несколько свечей (если много практик)
   - Перенесите подробности на сайт (QR код)
3. **Пересгенерируйте этикетки**

**Критические предупреждения** (> 800 символов):
- Текст может не поместиться
- **Обязательно** сократите или разделите на 2 свечи

## Технические детали

### Функции проверки:

```python
def check_overflow(candle: Candle) -> List[str]:
    """
    Проверяет переполнение текста
    Возвращает список предупреждений
    """
```

**Пороги:**
- `name`: 30 символов
- `description`: 250 символов
- `practice`: 400 символов
- `ritual_text`: 300 символов
- `total`: 800 символов (critical)

### Адаптивные классы:

```python
def get_text_size_class(text: str, thresholds: Dict) -> str:
    """
    Определяет CSS класс по длине текста:
    - text-short
    - text-medium
    - text-long
    - text-very-long
    - text-overflow
    """
```

### CSS классы:

**Этикетки:**
- `.label-name.long-title` - 11pt
- `.label-name.very-long-title` - 9pt

**Инструкции:**
- `.instruction-title h2.long-title` - 14pt
- `.instruction-title h2.very-long-title` - 11pt
- `.instruction-section.text-long p` - 8pt
- `.instruction-section.text-very-long p` - 7pt
- `.instruction-section.text-overflow p` - 6.5pt
- `.instruction-spell.text-long p` - 7.5pt
- `.instruction-spell.text-very-long p` - 6.5pt
- `.instruction-spell.text-overflow p` - 6pt

## Примеры

### Пример 1: Идеальный текст

```
Название: СВЕЧА ЛЮБВИ (12 символов) ✅
Описание: Привлекает любовь и гармонию в отношения. Раскрывает сердечную чакру. (78 символов) ✅
Практика: Зажгите свечу в новолуние. Визуализируйте встречу с любимым человеком. (82 символа) ✅
Заговор: Пламя горит, любовь манит. (32 символа) ✅
```
**Результат:** Без предупреждений, всё идеально!

### Пример 2: Длинный текст с адаптацией

```
Название: СВЕЧА ДЛЯ ОЧИЩЕНИЯ ПРОСТРАНСТВА ОТ НЕГАТИВА (45 символов) ⚠️
Описание: 180 символов ✅
Практика: 420 символов ⚠️
```
**Результат:**
- Предупреждение на первой странице
- Заголовок уменьшен до 9pt
- Практика уменьшена до 6.5pt
- **Всё поместится**, но читать сложнее

### Пример 3: Критическое переполнение

```
Название: 35 символов ⚠️
Описание: 280 символов ⚠️
Практика: 550 символов ⚠️
Заговор: 200 символов ✅
Total: 1030 символов 🚨
```
**Результат:**
- **КРИТИЧЕСКОЕ** предупреждение
- Текст может вылезти за края
- **Действие:** ОБЯЗАТЕЛЬНО сократить!

## Часто задаваемые вопросы

**Q: Можно ли отключить предупреждения?**
A: Нет, они всегда генерируются если есть проблемы. Просто проигнорируйте первую страницу при печати.

**Q: Насколько критичны предупреждения?**
A:
- < 800 символов: Текст поместится (хоть и мелкий)
- \> 800 символов: Высокий риск переполнения
- \> 1000 символов: Почти наверняка не поместится

**Q: Как система считает символы?**
A: `len(text)` в Python - все символы включая пробелы и знаки препинания.

**Q: Можно ли настроить пороги?**
A: Да, в файле `/var/www/labels/backend/label_generator.py`:
- Функция `check_overflow()` - пороги предупреждений
- Функция `get_text_size_class()` - пороги размеров шрифта

## Обновления

**v2.0 (2025-12-04):**
- ✅ Адаптивный размер шрифта
- ✅ Система предупреждений
- ✅ Страница предупреждений
- ✅ Умные CSS классы

**v1.0 (2025-12-03):**
- Базовая генерация этикеток
- Фиксированные размеры шрифта

---

*Документация создана: Claude Code Assistant*
